一、正常成功流程
例 1：缓存全命中（最短路径）
场景： 客户端查询 www.example.com / A，缓存中已有该 RRset 且未过期。

流程：

S0 START
  校验通过，初始化 ResolveCtx（current_name = www.example.com, qtype = A）

→ S1 CHECK_LOCAL_DATA
  查 RRset cache → 命中 www.example.com A 1.2.3.4（TTL 尚余 200s）
  分支 1：命中最终答案

→ S13 ASSEMBLE_SUCCESS
  Answer: www.example.com  A  1.2.3.4

→ S16 DONE

覆盖： S0 → S1(分支1) → S13 → S16

例 2：缓存命中 negative cache
场景： 客户端查询 nonexist.example.com / A，negative cache 中存有该名字的 NXDOMAIN 记录。

流程：

S0 START → S1 CHECK_LOCAL_DATA
  查 negative cache → 命中 NXDOMAIN（neg_ttl 尚余 60s）
  分支 3

→ S14 ASSEMBLE_NEGATIVE
  RCODE = NXDOMAIN，Authority 段附 SOA

→ S16 DONE

覆盖： S1(分支3) → S14 → S16

例 3：完整迭代解析（无 CNAME，从根开始）
场景： 客户端查询 www.example.com / A，缓存完全为空（冷启动）。

流程：

mipsasm
S0 START → S1 CHECK_LOCAL_DATA
  所有缓存未命中，分支 5

→ S2 PICK_STARTING_NS
  向上查：www.example.com → example.com → com → .
  都没有缓存 NS → ns_set = ROOT_HINTS，current_zone_cut = .

→ S3 ENSURE_NS_ADDRESS
  ROOT_HINTS 自带地址（如 198.41.0.4），无需子查询

→ S4 SELECT_SERVER
  选中 a.root-servers.net (198.41.0.4)

→ S5 SEND_QUERY
  向 198.41.0.4 发 UDP 查询 www.example.com/A，budget -= 1

→ S6 RECEIVE_RESPONSE
  收到响应

→ S7 PRECHECK_RESPONSE
  ID 匹配，TC=0，RCODE=NOERROR

→ S8 EXTRACT_ALIAS_CHAIN
  Answer 为空，无 alias → 分支 4

→ S9 CLASSIFY_RESPONSE
  Authority 含 com. NS a.gtld-servers.net，Additional 含其 A 记录
  → 情况 B：Referral

→ S10 HANDLE_DELEGATION
  ns_set = {a.gtld-servers.net, ...}，current_zone_cut = com.
  glue 在 bailiwick 内（gtld-servers.net 为 .net 下，由根给出的 glue 合理），缓存

→ S3 ENSURE_NS_ADDRESS
  已有 glue 地址

→ S4 SELECT_SERVER
  选中 a.gtld-servers.net (192.5.6.30)

→ S5 → S6 → S7 → S8（无 alias）→ S9
  收到第二次 referral：example.com. NS ns1.example.com
  Authority 含 NS，Additional 含 ns1.example.com A 93.184.216.34（in-bailiwick glue）
  → 情况 B

→ S10 HANDLE_DELEGATION
  ns_set = {ns1.example.com}，current_zone_cut = example.com.

→ S3 → S4 → S5 → S6 → S7 → S8（无 alias）→ S9
  Answer 含 www.example.com A 93.184.216.34
  → 情况 A：Final Positive Answer
  缓存 RRset

→ S13 ASSEMBLE_SUCCESS
  Answer: www.example.com  A  93.184.216.34

→ S16 DONE

覆盖： S1(分支5) → S2 → S3(直接有地址) → S4 → S5 → S6(收到响应) → S7(通过) → S8(分支4) → S9(情况B) → S10 → 循环两轮 → S9(情况A) → S13 → S16

例 4：缓存部分命中（已知委派点，缩短迭代）
场景： 查询 mail.example.com / MX，缓存中已有 example.com. NS ns1.example.com 及其地址。

流程：

stylus
S0 → S1 CHECK_LOCAL_DATA
  无 mail.example.com/MX 答案，无 alias，无 negative
  但命中最近已知委派：example.com. NS ns1.example.com
  分支 4

→ S2 PICK_STARTING_NS
  在 example.com. 这一级命中 NS → ns_set = {ns1.example.com}，zone_cut = example.com.

→ S3 ENSURE_NS_ADDRESS
  缓存中有 ns1.example.com A → 直接可用

→ S4 → S5 → S6 → S7 → S8 → S9
  Answer: mail.example.com MX 10 smtp.example.com
  → 情况 A

→ S13 → S16

覆盖： S1(分支4) → S2（深层缓存命中）

例 5：CNAME 跟踪 — 跨区解析
场景： 查询 www.a.com / A。www.a.com 是 CNAME 指向 cdn.b.net，cdn.b.net 有 A 记录。已知 a.com 和 b.net 的 NS。

流程：

mipsasm
S0 → S1（缓存未命中）→ S2 → S3 → S4 → S5 → S6 → S7

→ S8 EXTRACT_ALIAS_CHAIN
  Answer 中有：www.a.com CNAME cdn.b.net
  无目标类型 RRset → 分支 3
  更新 alias_chain = [www.a.com → cdn.b.net]
  current_name = cdn.b.net
  RCODE = NOERROR → 为新名字重新检查本地数据

→ S1 CHECK_LOCAL_DATA
  cdn.b.net/A 未缓存，但已知 b.net NS → 分支 4

→ S2 PICK_STARTING_NS
  ns_set = b.net 的 NS

→ S3 → S4 → S5 → S6 → S7 → S8（无 alias）→ S9
  Answer: cdn.b.net A 203.0.113.50
  情况 A

→ S13 ASSEMBLE_SUCCESS
  Answer 段:
    www.a.com    CNAME  cdn.b.net
    cdn.b.net    A      203.0.113.50

→ S16 DONE

覆盖： S8(分支3，NOERROR) → 回到 S1 重新解析新名字

例 6：缓存中已有 CNAME 链
场景： 查询 x.a.com / AAAA，缓存中已有 x.a.com CNAME y.b.com，y.b.com CNAME z.c.com，z.c.com AAAA 2001:db8::1。

流程：

stylus
S0 → S1 CHECK_LOCAL_DATA
  缓存命中 CNAME：x.a.com → y.b.com → z.c.com
  沿缓存链走到底，链尾 z.c.com/AAAA 也命中
  分支 2 → 链尾已命中最终 RRset

→ S13 ASSEMBLE_SUCCESS
  Answer:
    x.a.com    CNAME  y.b.com
    y.b.com    CNAME  z.c.com
    z.c.com    AAAA   2001:db8::1

→ S16 DONE

覆盖： S1(分支2，缓存 CNAME 链全部命中)

例 7：缓存中 CNAME 链部分命中
场景： 同上，但 z.c.com/AAAA 未缓存。

流程：

stylus
S0 → S1 CHECK_LOCAL_DATA
  缓存命中 CNAME 链 x.a.com → y.b.com → z.c.com
  但 z.c.com/AAAA 未命中
  分支 2 → 链尾未命中
  current_name = z.c.com

→ S2 PICK_STARTING_NS
  查找 z.c.com 的最近已知 NS …

→ （正常迭代解析 z.c.com/AAAA）

→ S13 ASSEMBLE_SUCCESS

覆盖： S1(分支2，缓存 CNAME 链尾未命中) → S2

例 8：QTYPE = CNAME 的查询
场景： 客户端执行 dig www.example.com CNAME，权威返回 www.example.com CNAME cdn.example.net。

流程：

mipsasm
S0 START
  original_qtype = CNAME

→ S1（未缓存）→ S2 → S3 → S4 → S5 → S6 → S7

→ S8 EXTRACT_ALIAS_CHAIN
  前置判断：original_qtype = CNAME
  Answer 中有 www.example.com CNAME cdn.example.net
  → 该 CNAME 记录本身即为最终答案，不跟踪

→ S13 ASSEMBLE_SUCCESS
  Answer: www.example.com  CNAME  cdn.example.net

→ S16 DONE

覆盖： S8 前置判断（QTYPE=CNAME 特殊处理）

例 9：权威回复 NODATA
场景： 查询 example.com / AAAA，权威只配了 A，没有 AAAA。

流程：

armasm
S0 → S1 → S2 → … → S5 → S6 → S7 → S8（无 alias）

→ S9 CLASSIFY_RESPONSE
  RCODE = NOERROR，Answer 为空，Authority 含 SOA（无 NS referral）
  → 情况 C：Negative Answer（NODATA）

→ S12 HANDLE_NEGATIVE
  提取 SOA，neg_ttl = min(SOA.TTL, SOA.MINIMUM)
  negative_result = NODATA

→ S14 ASSEMBLE_NEGATIVE
  RCODE = NOERROR（客户端通过空 Answer + SOA 识别为 NODATA）

→ S16 DONE

覆盖： S9(情况C, NODATA) → S12 → S14

例 10：权威回复 NXDOMAIN
场景： 查询 doesnotexist.example.com / A。

流程：

… → S9 CLASSIFY_RESPONSE
  RCODE = NXDOMAIN，Authority 含 SOA
  → 情况 C

→ S12 HANDLE_NEGATIVE
  negative_result = NXDOMAIN，缓存

→ S14 ASSEMBLE_NEGATIVE
  RCODE = NXDOMAIN

→ S16 DONE

覆盖： S9(情况C, NXDOMAIN) → S12 → S14

例 11：TC=1 截断，回退 TCP
场景： 查询 example.com / TXT，权威回复的 TXT 记录较大，UDP 响应被截断。

流程：

armasm
… → S5 SEND_QUERY (UDP)
→ S6 收到响应
→ S7 PRECHECK_RESPONSE
  TC = 1，当前 transport = UDP
  → 切换 TCP

→ S5 SEND_QUERY (TCP)
→ S6 收到完整响应
→ S7 TC = 0，RCODE = NOERROR
→ S8 → S9 → 情况 A

→ S13 → S16

覆盖： S7(TC=1, UDP→TCP) → S5

例 12：NS 地址需要子查询
场景： 查询 www.foreign.jp / A。收到 referral 到 foreign.jp. NS ns1.otherdomain.co.jp，但 Additional 中无 glue（out-of-bailiwick），缓存中也没有 ns1.otherdomain.co.jp 的地址。

流程：

mipsasm
… → S10 HANDLE_DELEGATION
  ns_set = {ns1.otherdomain.co.jp}
  Additional 中的 glue 为 out-of-bailiwick → 不采信

→ S3 ENSURE_NS_ADDRESS
  ns1.otherdomain.co.jp 无缓存地址 → 触发子查询

→ S11 RESOLVE_NS_ADDRESS_SUBQUERY
  subquery_depth = 1（≤ 5，OK）
  递归解析 ns1.otherdomain.co.jp/A → 得到地址 203.0.113.10
  写入 ns_addr_cache → 返回 S3

→ S3 现在有地址
→ S4 → S5 → …（继续正常解析）

覆盖： S10(out-of-bailiwick glue 拒绝) → S3(无地址) → S11 → 返回 S3

例 13：CNAME + NXDOMAIN 同一响应
场景： 查询 alias.example.com / A，权威回复中 Answer 含 alias.example.com CNAME target.example.com，但 RCODE = NXDOMAIN 且 Authority 含 SOA（表示 target.example.com 不存在）。

流程：

stylus
… → S8 EXTRACT_ALIAS_CHAIN
  提取 alias_chain: alias.example.com → target.example.com
  链尾无目标 RRset → 分支 3
  RCODE = NXDOMAIN 且 Authority 含 SOA
  → 缓存 target.example.com 的 NXDOMAIN negative

→ S14 ASSEMBLE_NEGATIVE
  Answer 段保留 CNAME 链:
    alias.example.com  CNAME  target.example.com
  RCODE = NXDOMAIN，Authority 含 SOA

→ S16 DONE

覆盖： S8(分支3，CNAME + NXDOMAIN) → S14

二、错误 / 失败流程
例 14：畸形查询 — S0 拒绝
场景： 收到一个 QDCOUNT = 0 的查询报文。

流程：

S0 START
  基本校验：QDCOUNT ≠ 1 → 不通过
  返回 FORMERR

→ S16 DONE

覆盖： S0 校验失败 → S16

例 15：超时重试后换服务器
场景： 查询 www.example.com / A。第一个 NS 超时，重试一次仍超时，换第二个 NS 成功。

流程：

apache
… → S4 SELECT_SERVER
  选中 ns1.example.com (10.0.0.1)，current_server_retries = 2

→ S5 SEND_QUERY → S6 RECEIVE_RESPONSE
  超时，current_server_retries = 1 > 0
  → 标记降权，回 S5

→ S5 → S6
  再次超时，current_server_retries = 0
  → 回 S4

→ S4 SELECT_SERVER
  ns1 已在 tried_servers，选中 ns2.example.com (10.0.0.2)

→ S5 → S6（收到响应）→ S7 → … → 正常完成

覆盖： S6(超时, retries>0) → S5 → S6(超时, retries=0) → S4(换 server)

例 16：上游返回 SERVFAIL → 换服务器
场景： 查询到 example.com 的 NS，第一个返回 SERVFAIL，第二个正常。

流程：

… → S5 → S6（收到响应）

→ S7 PRECHECK_RESPONSE
  RCODE = SERVFAIL
  标记 bad_servers[ns1] → S4

→ S4 SELECT_SERVER
  选中 ns2

→ S5 → S6 → S7（RCODE = NOERROR）→ …正常

覆盖： S7(RCODE = SERVFAIL) → S4

例 17：Response ID 不匹配
场景： 向 NS 发出查询 ID=0x1234，收到一个 ID=0xABCD 的响应（可能是伪造或乱序）。

流程：

… → S5 SEND_QUERY (ID=0x1234)
→ S6 收到响应

→ S7 PRECHECK_RESPONSE
  response.ID (0xABCD) ≠ query.ID (0x1234)
  丢弃该响应 → 回 S6 继续等待

→ S6
  超时窗口内未收到正确响应 → 视为超时
  current_server_retries > 0 → S5
  或 retries = 0 → S4

覆盖： S7(ID 不匹配) → S6 → 超时路径

例 18：CNAME 循环检测
场景： a.com CNAME b.com，b.com CNAME a.com。客户端查询 a.com / A。

流程：

stylus
S0 → S1 → S2 → … → S8
  提取 alias: a.com → b.com
  cname_visited = {a.com}，current_name = b.com

→ S1 → S2 → … → S8
  提取 alias: b.com → a.com
  检查 cname_visited → a.com 已存在！
  分支 1：alias loop

→ S15 FAIL_SERVFAIL

→ S16 DONE

覆盖： S8(分支1, alias loop) → S15

例 19：CNAME 链深度超限
场景： 存在 c1 → c2 → c3 → … → c9（每个都是 CNAME），max_alias_depth = 8。

流程：

经过 8 次 alias 追踪后，遇到第 9 个 CNAME

→ S8 EXTRACT_ALIAS_CHAIN
  alias_chain.length = 9 > max_alias_depth(8)
  分支 1：depth 超限

→ S15 FAIL_SERVFAIL → S16 DONE

覆盖： S8(分支1, depth 超限) → S15

例 20：Referral 循环检测
场景： 查询 x.example.com / A。example.com NS 返回 referral 到 example.com 自己（错误配置），导致反复收到相同 referral。

流程：

→ S10 HANDLE_DELEGATION (第一次)
  referral_history 记录 (x.example.com, example.com, {ns1.example.com})

→ S3 → S4 → S5 → … → S9 情况 B

→ S10 HANDLE_DELEGATION (第二次)
  检查 referral_history → 等价签名已出现且无进展
  → referral loop 检测触发

→ S15 FAIL_SERVFAIL → S16 DONE

覆盖： S10(referral loop) → S15

例 21：Query budget 耗尽
场景： 解析过程中经历大量 referral、NS 故障、重试，budget 从 50 降到 0。

流程：

… → S5 SEND_QUERY
  total_query_budget -= 1 → budget = 0
  budget ≤ 0

→ S15 FAIL_SERVFAIL → S16 DONE

覆盖： S5(budget 耗尽) → S15

例 22：子查询深度超限
场景： 解析 NS 地址时需要再解析另一个 NS 的地址，层层嵌套超过 max_subquery_depth = 5。

流程：

→ S3 → S11 (depth=1)
  内部又需解析另一个 NS → S11 (depth=2)
  …
  → S11 (depth=6)
  subquery_depth(6) > max_subquery_depth(5)
  失败 → 逐层返回 S3

→ S3 所有 NS 解析失败

→ S15 FAIL_SERVFAIL → S16 DONE

覆盖： S11(depth 超限) → S3(全部失败) → S15

例 23：所有 NS 不可达
场景： example.com 有两个 NS，均超时或返回 REFUSED，且无更多可选 server。

流程：

armasm
→ S4 选中 ns1 → S5 → S6 超时（重试用尽）→ S4
→ S4 选中 ns2 → S5 → S6 → S7 RCODE=REFUSED → S4
→ S4 无更多可选 server

→ S15 FAIL_SERVFAIL → S16 DONE

覆盖： S4(无可选 server) → S15

例 24：空响应 / 不可分类异常
场景： NS 返回 RCODE=NOERROR，但 Answer 为空、Authority 中也没有 NS 也没有 SOA。（配置错误的权威服务器）

流程：

… → S8（无 alias）

→ S9 CLASSIFY_RESPONSE
  不符合 A（无答案）、B（无 NS referral）、C（无 SOA 无法确认 negative）
  → 情况 D：不可分类
  标记 current_server 低质量

→ S4 SELECT_SERVER（尝试其他 NS）

覆盖： S9(情况D) → S4

例 25：TC=1 + TCP 仍异常
场景： UDP 收到 TC=1，切 TCP 后连接被重置。

流程：

→ S7 TC=1, transport=UDP → 切 TCP
→ S5 (TCP) → S6 网络失败（连接重置）
  retries 用尽 → S4（标记 server failure）

→ S4 → 选其他 server 或无可选 → S15

覆盖： S7(TC=1→TCP) → S5 → S6(TCP 失败) → S4